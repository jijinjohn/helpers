// basic configs
git config --global user.name "Jijin John Manalikkal"
git config --global user.email "jijinjohn@gmail.com"
git config --global merge.tool meld
git config --global core.editor vim

// check values of config keys
git config user.name
git config --list

git help <verb>

// From http://git-scm.com/book/en/Git-Basics-Viewing-the-Commit-History
/*
One of the more helpful options is -p, which shows the diff introduced in each
commit. You can also use -2, which limits the output to only the last two
entries:
*/
git log -p -2

git commit --amend

// shows you the URL that Git has stored for the shortname to be expanded to:
git remote -v

// git push [remote-branch] [local-branch]
git push origin master

git clone https://github.com/jijinjohn/helpers
